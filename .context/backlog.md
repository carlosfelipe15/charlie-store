# 📋 Backlog Dinámico de Desarrollo

## 🚨 Errores y Bugs Activos (Bloqueantes)

*   [ ] **[BUG/ADMIN-INDEX]** `GET /admin/products` con cualquier filtro (`status[]`, búsqueda, etc.) devuelve un `count` incorrecto (más bajo que el real), aunque los datos en sí vienen completos si se pide un `limit` suficientemente alto.
    - *Detectado el:* 2026-07-12 por Claude, investigando el reporte de Carlos de no ver productos nuevos en el admin
    - *Contexto:* No es el mismo bug que `[DATA/INDEX-STALE]` (ya resuelto abajo) — ese era el índice desactualizado; esto persiste **después** de un reindex completo y correcto (`Product` indexado 54/54 filas reales). Con `GET /admin/products?limit=1&status[]=draft&status[]=proposed&status[]=published&status[]=rejected` (los 4 status válidos, o sea "sin filtrar" en efecto) el `count` reportado fue 29 en vez de 54; pidiendo `limit=100` con el mismo filtro, sí vinieron las 54 filas completas — el bug es solo en el cálculo de `count`, no en los datos. Sin filtro (`GET /admin/products?limit=1`), el `count` es correcto (54). Parece un bug de core de Medusa 2.15.2 en `getProductsWithIndexEngine()` (mismo módulo/familia que el bug de `category_id`/`tag_id` ya documentado en `AGENTS.md`), no algo introducido por este repo.
    - *Impacto:* Si el admin dashboard aplica algún filtro (`status`, búsqueda) — cosa que sí ocurre en flujos normales de uso, aunque la carga inicial sin filtro no lo dispara — la paginación puede calcular mal el total de páginas y ocultar productos reales dentro del rango visible.
    - *Posible solución:* Igual que se hizo para el store (`/store/products-list`, ver sección "Índice de búsqueda cross-módulo" en `AGENTS.md`), requeriría una ruta admin propia que resuelva ids vía `query.index()` pero calcule el conteo final vía `query.graph()` — o reportarlo como bug upstream a Medusa. No se implementó en esta pasada por ser cambio de mayor alcance (ruta admin custom) fuera del pedido original (arreglar por qué el filtro de marca daba 0).

## ✅ Resueltos Recientemente
*   [x] **[PERF/STOREFRONT-NAV]** La navegación del storefront se sentía lenta al abrir la PDP y el listado de productos de una categoría.
    - *Resuelto el:* 2026-07-14 por Claude, a pedido de Carlos
    - *Método:* Se descartó primero lo que **no** era: se midió el backend con `curl` (todas las queries por debajo de ~210ms, catálogo actual de 48 productos, máx. 10 por categoría) y se confirmó que Redis **no** es la causa — el backend no tiene Redis configurado (`REDIS_URL` existe en `.env` pero `medusa-config.ts` no lo usa; corre con cache/event-bus/workflow-engine en memoria), pero para un solo proceso el cache en memoria es *más* rápido que Redis (sin salto de red), y los cuellos encontrados son de otra naturaleza. El middleware tampoco (regiones memoizadas con TTL 1h). Se aisló el problema real cargando las páginas en un navegador (Playwright) y midiendo con `performance.getEntriesByType()`.
    - *Causa raíz #1 (lo que dominaba) — N+1 de Server Actions en la grilla:* cada `RodiProductCard` disparaba en un `useEffect` al montar una Server Action (`getCartLineForVariant`) que hacía un `retrieveCart()` **completo** (carrito entero con `*items.product`, `*items.variant`, inventario, promociones) solo para saber la cantidad de una variante. Next **serializa las Server Actions una a una** (no van en paralelo), así que un listado de 10 productos disparaba 11 POST en fila india **después** de que el HTML ya estaba pintado. Medido en `/es/categories/frescos`: HTML listo en ~0.7s pero la página seguía trabajando hasta ~2.75s (`maxEnParalelo: 1`, ráfaga de ~2.25s). Confirmado que es real en producción, no un artefacto de dev (el conteo no se duplicaba por Strict Mode — predicción inicial equivocada, verificada como falsa).
    - *Causa raíz #2 — `*products` en el header en cada navegación:* `listCategories()` (llamado por el header en toda página) pedía por defecto `*products` sobre hasta 100 categorías, expandiendo la lista completa de productos de cada una. Nadie consumía esa data (el único lector era un fallback de conteo de skeletons que ya defaulteaba). Payload del backend: **278KB → 32KB (−88%)**, que además viaja serializado como RSC en cada navegación.
    - *Causa raíz #3 — over-fetch del listado:* `listProductsWithSort` traía **100 productos con precios calculados + inventario** para mostrar 12, ordenando en memoria y descartando 88. Latente hoy (el catálogo cabe en 100) pero escala mal.
    - *Fix #1 (cart-line):* nuevo helper puro `lib/util/cart-line.ts` (`buildCartLineMap`/`getCartLineForVariant`). `lib/data/cart-line.ts` dejó de ser `"use server"`: ahora expone `getCartLineMap` memoizado con `cache()` de React (una grilla de N tarjetas = **una** carga de carrito por render). `ProductPreview` (el único componente por el que pasan las 5 grillas — categoría, home, relacionados, favoritos, destacados) resuelve la línea en el servidor y la pasa como prop. `RodiProductCard` perdió el `useEffect`+`syncCartLine`; el estado viene por prop con actualización optimista al click + re-sync vía `router.refresh()`. `addToCart` ahora **devuelve la línea creada** (aprovechando el carrito que la mutación ya retorna), para mostrar los controles +/− al instante sin round-trip extra. Resultado medido: **11 Server Actions → 0**, todo asentado en ~0.86s (antes ~2.75s).
    - *Fix #2 (`*products`):* `listCategories`/`getCategoryByHandle` en `lib/data/categories.ts` pasaron a `fields: "*category_children, *parent_category"` (sin `*products`, sin el nieto `*parent_category.parent_category` que tampoco se usaba). El conteo del skeleton en `categories/templates/index.tsx` pasó a fijo (`8`). **Trampa encontrada y evitada:** el primer intento usó `"id, name, handle, *category_children, *parent_category"` y **rompió las subcategorías** — `parent_category` es un link self-referencial, y mezclar escalares raíz explícitos con un wildcard `*` sobre un link hace que Medusa devuelva el link como **NULL sin error**. La forma correcta es wildcard-only (los escalares raíz vienen por defecto). Aislado con una tabla de combinaciones contra el backend.
    - *Fix #3 (over-fetch):* `listProductsWithSort` refactorizado: `created_at` (orden por defecto, caso dominante) usa **paginación real del backend** (`limit=12`, `offset`, `order=-created_at`); solo `price_asc`/`price_desc` mantienen el over-fetch acotado (`PRICE_SORT_WINDOW=100`) + orden en memoria, porque `calculated_price` lo computa el Pricing module por región y no es una columna ordenable. Se quitó el bloque `order` redundante de `paginated-products.tsx`.
    - *Verificado (Playwright + logs `logging.fetches.fullUrl`):* listado por defecto pide `limit=12&offset=0&order=-created_at`; página 2 pide `offset=12` (12 productos distintos, sin solapamiento); orden por precio ↑ monótono correcto (€0.73→€2.75); categoría `frescos` con 10 productos y subcategorías intactas; subcategoría `frescos-verduras-hortalizas` con chips de hermanos y `parent_category` poblado; grilla de categoría con **0 Server Actions** tras pintar; add/increase/decrease + persistencia tras recarga OK. Typecheck limpio, 0 errores de consola en todas las páginas. `next.config.js` sigue con `images.unoptimized: true` (empeora lo percibido en la grilla pero es secundario; no se tocó).
    - *Archivos:* `apps/storefront/src/lib/util/cart-line.ts` (nuevo), `apps/storefront/src/lib/data/cart-line.ts`, `apps/storefront/src/lib/data/cart.ts`, `apps/storefront/src/lib/data/categories.ts`, `apps/storefront/src/lib/data/products.ts`, `apps/storefront/src/modules/products/components/product-preview/index.tsx`, `apps/storefront/src/modules/products/components/rodi-product-card/index.tsx`, `apps/storefront/src/modules/store/templates/paginated-products.tsx`, `apps/storefront/src/modules/categories/templates/index.tsx`.
    - *Gap que sigue abierto:* el over-fetch de precio (`PRICE_SORT_WINDOW=100`) da orden aproximado si una sola lista supera 100 productos — aceptable hoy; revisar si el backend gana orden por precio o si algún catálogo crece más allá. Y `next.config.js` con `images.unoptimized: true` (ver también el ítem de `ignoreBuildErrors` en `index.md`).
*   [x] **[UI/PDP-GALERIA]** La imagen principal de la PDP era demasiado grande para productos de almacén/grocery — mismo tratamiento (imagen a todo el ancho disponible sin tope, `object-cover`) para una botella de gaseosa que para una campera.
    - *Resuelto el:* 2026-07-14 por Claude, a pedido de Carlos
    - *Causa:* `rodi-image-gallery/index.tsx` + `templates/index.tsx` (grid `minmax(0,1fr)_420px`) no distinguían categoría de producto — todo el catálogo usaba el mismo layout, equivalente al PDPv1/v2 ("hero", imagen grande) del diseño de referencia (`pdp.jsx`). Ese mismo archivo define una tercera variante explícita, PDPv3 ("Grocery-density"), pensada para ítems de reposición rápida con imagen chica y contenida — nunca se había implementado. Confirmado en vivo con Playwright antes del fix: la imagen de "Soft Drinks" (una gaseosa) renderizaba a 831×831px (58% del viewport en 1440px), igual que la de "Medusa Sweatshirt".
    - *Fix:* nueva función `shouldUseCompactGallery(product)` en `apps/storefront/src/lib/util/product.ts` — galería compacta si el producto **(a)** pertenece a una de las 13 categorías curadas de Rodi Mercado (`CURATED_CATEGORY_HANDLES`, ahora exportado desde `apps/storefront/src/lib/util/category-emoji.ts`) excluyendo `electrodomesticos`, **y (b)** no tiene opciones de variante tipo Talla/Size/Color (regex sobre `option.title`). `Electrodomésticos` queda deliberadamente fuera pese a no tener variantes de talla/color: el propio diseño de referencia usa un air fryer como ejemplo canónico de PDPv1/v2 (imagen grande), y en ecommerce real los electrodomésticos son "consideration purchase" (se inspecciona diseño/tamaño antes de comprar) — no una recompra rápida de commodity como sí lo es un producto de almacén.
    - `lib/data/products.ts` ahora pide `+categories.id,+categories.handle` en el fetch de producto (antes `product.categories` no se traía en ningún punto del storefront). `rodi-image-gallery/index.tsx` acepta un nuevo prop `compact`: limita el ancho de la imagen principal a `420px` (mismo valor que la columna de info original), aumenta el padding interno y cambia `object-cover` → `object-contain` para no recortar el packshot.
    - *Ajuste de seguimiento (mismo día, a pedido de Carlos):* la primera versión dejaba la columna de imagen en `minmax(0,1fr)` (sin cambios) con solo la imagen misma achicada — esto dejaba un espacio vacío grande entre la imagen chica y la columna de info (~430px en viewport 1440px), con la imagen pegada al borde izquierdo. Fix: en modo compacto, `templates/index.tsx` cambia el grid del `<main>` a columnas de ancho fijo ajustado al contenido (`grid-cols-[minmax(0,504px)_minmax(0,480px)]`, 504px = ancho exacto de la galería con miniaturas) más `justify-center`, en vez de mantener la proporción `1fr` / `420px` fija del layout hero. Esto centra el bloque de dos columnas dentro del `content-container` (imagen deja de estar pegada al borde) y la columna de info se ensancha moderadamente (420px → 480px) y se desplaza hacia la izquierda para ocupar el espacio liberado, sin estirar los componentes de precio/carrito a un ancho exagerado. El layout hero (apparel/electrodomésticos) no se tocó.
    - *Verificado:* Playwright en 1024px/1280px/1440px y 390px — "Soft Drinks" (bebidas, sin variantes) y "Rice" (despensa) renderizan con galería compacta y centrada, sin espacio vacío ni overflow en ningún breakpoint; "Medusa Sweatshirt" (Shirts, con opción Size) y "Microwave Oven" (electrodomesticos, sin variantes) siguen con la galería grande sin cambios.
    - *Archivos:* `apps/storefront/src/lib/util/product.ts`, `apps/storefront/src/lib/util/category-emoji.ts`, `apps/storefront/src/lib/data/products.ts`, `apps/storefront/src/modules/products/components/rodi-image-gallery/index.tsx`, `apps/storefront/src/modules/products/templates/index.tsx`.
*   [x] **[DATA/INDEX-STALE]** Filtrar productos por marca en el storefront devolvía "0 productos" para las marcas agregadas por `seed-mercado-catalog.ts`, y el admin no mostraba los productos nuevos apenas se aplicaba cualquier filtro (status, búsqueda).
    - *Resuelto el:* 2026-07-12 por Claude, a pedido de Carlos
    - *Causa raíz:* el Index Engine (`@medusajs/index`, ver "Índice de búsqueda cross-módulo" en `AGENTS.md`) mantiene sus tablas (`index_data`/`index_metadata`/...) sincronizadas vía eventos del event bus — en dev, el Local Event Bus ("not recommended for production"). Confirmado con SQL directo: `index_metadata.status = "done"` para las 9 entidades indexadas, pero `index_data` tenía solo 10 filas de `Product` y 4 de `Brand` contra 54 y 16 reales — todo lo creado desde el 2026-07-08 (incluyendo los 44 productos y 13 marcas de `seed-mercado-catalog.ts`, corrido vía `medusa exec`) nunca se indexó. `status: "done"` solo refleja que el *schema* indexado no cambió, no que las filas estén al día — falsa sensación de estar sincronizado. Causa de fondo: un script `medusa exec` es un proceso corto que termina apenas resuelve su función default, y los listeners que el Index Engine agenda de forma asíncrona en respuesta a los eventos de creación pueden no llegar a correr antes de que el proceso termine — a diferencia de un `medusa develop` de larga duración, donde sí corren.
    - *Fix:* nuevo script `apps/backend/src/scripts/reindex-search.ts` (correr con `pnpm medusa exec ./src/scripts/reindex-search.ts`) que trunca las 4 tablas del Index Engine y dispara `indexModuleService.sync({strategy: "full"})`. Nota de implementación no obvia: llamar `sync()` sin truncar antes **no alcanza** — el chequeo interno (`checkChanges()`) solo re-sincroniza una entidad si el *hash del schema* indexado cambió, no si las filas están desactualizadas, así que sin el truncate previo no hay manera de forzar un resync completo por API pública. Tampoco alcanza con correr el script `medusa exec` solo — en ese contexto el módulo corre en rol "server" (no "worker"), así que ni siquiera el resync se ejecuta de verdad ahí; hace falta que el proceso de `medusa develop` real (que sí corre en modo worker) arranque *después* del truncate para que su `onApplicationStart_` haga el backfill real. `seed-mercado-catalog.ts` ahora loguea un recordatorio de correr este script al final si creó productos/marcas nuevos.
    - *Verificado:* antes del fix, `GET /store/products-list?brand_id=<Chanel>` devolvía `count: 0`; después, `count: 1` con el producto correcto. `GET /admin/products` con filtro de status pasó de `count: 1` a `count: 29` (dato completo confirmado con `limit=100`, ver bug activo `[BUG/ADMIN-INDEX]` arriba sobre por qué el `count` en sí sigue sin ser exacto). `index_data` post-fix: `Product` 54/54, `Brand` 16/16 (match exacto con conteo real de las tablas `product`/`brand`).
    - *Gap relacionado, no resuelto:* ver `[BUG/ADMIN-INDEX]` arriba — el `count` del admin sigue siendo inexacto bajo filtros incluso con el índice al día; es un bug distinto (de cálculo, no de staleness).
*   [x] **[DATA/CATALOGO]** No había productos reales en las categorías de Rodi Mercado — solo el catálogo demo de ropa del starter de Medusa (Shirts/Sweatshirts/Pants/Merch), las 14 categorías reales estaban vacías.
    - *Resuelto el:* 2026-07-11 por Claude, a pedido de Carlos
    - *Fix:* Nuevo script `apps/backend/src/scripts/seed-mercado-catalog.ts` (correr con `pnpm medusa exec ./src/scripts/seed-mercado-catalog.ts` desde `apps/backend`; idempotente — salta handles ya existentes). Carga 44 productos reales tomados de `dummyjson.com/products` (imágenes, SKU, stock, descripciones reales) across sus categorías `groceries`, `beauty`, `fragrances`, `kitchen-accessories` y `skin-care`: 27 en categorías de mercado (Frutas y verduras, Despensa, Lácteos y huevos, Carnes y pescados, Bebidas, Congelados, Mascotas, Limpieza del hogar), 4 en Electrodomésticos (los únicos ítems realmente "electrodoméstico" de `kitchen-accessories` — el resto de esa categoría de origen es utensilios de cocina sin categoría propia en Rodi Mercado, se descartaron) y 13 con marca real bajo Aseo personal (10, beauty/fragrances: Chanel, Dior, Calvin Klein, Gucci, etc.) y Farmacia (3 de `skin-care` — Attitude, Olay, Vaseline — + Protein Powder de groceries, sin marca). Las marcas se eligieron deliberadamente para tener datos realistas con los que probar el filtro de Brands (Fase 10): crea las 13 marcas correspondientes (`createBrandWorkflow`, reusa si ya existe por nombre), asigna `additional_data.brand_id` en la creación de producto (nota: `createProductsWorkflow` solo acepta un `brand_id` compartido por llamada, así que los 13 productos con marca se crean uno a la vez; los 31 sin marca van en un solo batch), carga niveles de inventario reales (`stock` del JSON de origen, no una cifra arbitraria) y siembra ~130 reseñas (`createReviewWorkflow`, `customer_id` = email del reviewer del JSON — texto libre, no requiere clientes reales) usando el array `reviews` de cada producto de origen.
    - *Decisiones tomadas con Carlos:* mantener el catálogo demo de ropa sin tocar (no se pidió eliminarlo); imágenes hotlinkeadas directo a `cdn.dummyjson.com` (sin descargar/rehostear) — funciona sin tocar `next.config.js` porque `images.unoptimized: true` ya está seteado globalmente en el storefront (Next sirve el `src` tal cual, sin pasar por el loader que valida `remotePatterns`); furniture y home-decoration (categorías completas de origen) descartadas por no encajar en ninguna categoría existente.
    - *Verificado:* corridas repetidas del script confirman idempotencia ("ya está sembrado, nada que crear"); query directa a la BD confirmó categoría/marca/precios(eur+usd)/stock correctos en las 11 categorías con productos; con backend+storefront corriendo localmente, `/es/categories/frescos` renderiza los productos e imágenes reales de `cdn.dummyjson.com`, y `/es/products/chanel-coco-noir-eau-de` muestra la marca "Chanel" y la sección de reseñas poblada.
    - *Gap que sigue abierto (definitivo, no por falta de páginas):* Panadería, Snacks y dulces y Bebé siguen sin productos — se recorrió el catálogo completo de dummyjson.com (`GET /products/categories`, 24 categorías) y ninguna cubre bakery/snacks/candy/baby; haría falta otra fuente de datos para esas 3 categorías.
*   [x] **[DATA/FAVORITES+REVIEWS]** `create-favorite` y `create-review` no validaban que `product_id` correspondiera a un producto real; se podían crear favoritos/reseñas + link huérfano apuntando a un producto inexistente.
    - *Resuelto el:* 2026-07-10 por Claude
    - *Detectado durante:* pasada de testing manual de endpoints (ver sección de bugs de esa sesión) — confirmado con `curl` que `POST /store/favorites` y `POST /store/reviews` aceptaban cualquier `product_id`, incluso uno que no existía en la BD.
    - *Fix:* nuevo step compartido `workflows/steps/validate-product-exists.ts` (usa `retrieveProduct`, que lanza `NOT_FOUND` automático) invocado al inicio de `create-favorite.ts` y `create-review.ts`. Además se agregó `DELETE /store/reviews/:id` (workflow `delete-review.ts` + steps `find-review.ts`/`delete-review.ts`), que no existía — reviews solo tenía `GET`/`POST` hasta ahora. El delete valida ownership (`review.customer_id === auth_context.actor_id`) y **no** es idempotente (404 si no existe, 400/`NOT_ALLOWED` si es de otro cliente), a diferencia de `delete-favorite` que sí lo es. Detalle en `docs/custom-features/favorites.md` y `docs/custom-features/reviews.md`.
    - *Verificado:* vía `curl` — `product_id` inexistente rechazado con 404 en ambos endpoints; producto real sigue funcionando; DELETE de review propia funciona, de review ajena rechaza (400), de review inexistente 404 (verificado con un segundo customer de prueba).
*   [x] **[UI/REGION]** El selector de región/país (`CountrySelect`) no era accesible en desktop.
    - *Resuelto el:* 2026-07-09 por Claude
    - *Causa:* El único punto de acceso a `CountrySelect` era el `SideMenu`, renderizado con `small:hidden` (solo mobile). El bloque "Entregar en / Tu dirección" del header desktop era texto estático sin `onClick`.
    - *Fix:* Se reemplazó el bloque estático por `CountrySelect` real en `rodi-header-client.tsx`, con su propio `useToggleState()` independiente del de `SideMenu` (que sigue sirviendo mobile sin cambios), y se cambió el copy "Shipping to:"/"Tu dirección" por "Región:" para no prometer un picker de direcciones que no existe. Verificado con Playwright: el dropdown abre y lista las 7 regiones configuradas (Denmark, France, Germany, Italy, Spain, Sweden, United Kingdom).
*   [x] **[UI/HEADER]** El bloque "Entregar en / Tu dirección" del header era decorativo.
    - *Resuelto el:* 2026-07-09 por Claude
    - *Fix:* Resuelto junto con el ítem anterior — el bloque ahora es el selector de región real, no un picker de direcciones (no existe infraestructura de direcciones guardadas en el header; se decidió no construirla en esta pasada).
*   [x] **[UI/HOME]** El newsletter del home mostraba un toast de éxito sin persistir el email.
    - *Resuelto el:* 2026-07-09 por Claude
    - *Fix:* Se ajustó el copy del toast en `rodi-newsletter/index.tsx` (tipo `info`, no `success`) para que ya no implique que la suscripción quedó registrada. La implementación real del backend queda trackeada aparte en `[FEATURE/NEWSLETTER]` (ver abajo), ya que requiere una decisión de negocio (proveedor de email marketing) fuera de alcance de este fix.
*   [x] **[UI/HOME]** Secciones del home (flash sale / curated rows) se ocultaban sin fallback cuando faltaba data.
    - *Resuelto el:* 2026-07-09 por Claude
    - *Causa:* Confirmado que no era un bug de código — ambos componentes ya devuelven `null` intencionalmente sin price lists de oferta, y el seed no crea ninguna.
    - *Fix:* Documentado en [docs/development.md](docs/development.md) un runbook para crear una Price List de oferta desde el Admin y poder ver la sección poblada en local. No se tocó código.
*   [x] **[FEATURE/FAVORITOS]** Implementado el feature completo de favoritos/wishlist.
    - *Resuelto el:* 2026-07-09 por Claude
    - *Qué se construyó:* Backend siguiendo el patrón Module → Link → Workflow → API (módulo `favorite`, link `product↔favorite`, workflows `create-favorite`/`delete-favorite` idempotentes, rutas `GET/POST /store/favorites` + `DELETE /store/favorites/:product_id`, todas autenticadas). Storefront: toggle de corazón en product card y galería de PDP, ícono con badge en el header, página `/account/favorites`, entradas en nav de cuenta y footer. Detalle completo en [.context/plans/2026-07-08/FASE-9-favoritos.md](.context/plans/2026-07-08/FASE-9-favoritos.md) y [docs/custom-features/favorites.md](docs/custom-features/favorites.md).
    - *Verificado:* backend end-to-end vía curl (401 sin auth, creación/duplicado idempotente, borrado/borrado repetido idempotente) y storefront vía Playwright (toggle logueado/deslogueado, badge del header actualizándose, página de cuenta reflejando cambios).
*   [x] **[UI/HEADER]** Warning de consola "Each child in a list should have a unique key prop" en `RodiHeaderClient`.
    - *Resuelto el:* 2026-07-08 por Claude
    - *Causa:* `RodiHeader` ([index.tsx](apps/storefront/src/modules/layout/components/rodi-header/index.tsx)) le pasa a `RodiHeaderClient` (Client Component) dos slots de Server Components (`accountSlot`, `cartSlot`) como props separadas — el patrón estándar de Next.js App Router para inyectar Server Components en un Client Component. Al recibir más de un slot renderizado en servidor sin `key` explícita en su elemento raíz, React/Next.js los trataba como lista al serializarlos a través del boundary RSC → cliente, y disparaba el warning (sin impacto funcional visible, pero con riesgo de pérdida de estado interno de esos elementos entre reconciliaciones).
    - *Fix:* se agregó `key="account-slot"` al `<Suspense>` que envuelve `RodiAccountButton` y `key="cart-slot"` a `<CartDropdown>` en `rodi-header/index.tsx`. Verificado con Playwright en `/es/store`: 1 error de consola antes del cambio, 0 después (dos recargas).

## ✅ Resueltos Recientemente (cont.)
*   [x] **[DATA/SUBCATEGORIAS]** Las categorías no tenían subcategorías — el diseño (`data.jsx` `MEGAMENU`, `pages.jsx` chips de Despensa) especifica una jerarquía de 2 niveles que no existía en la BD.
    - *Resuelto el:* 2026-07-10 por Claude
    - *Causa:* Los 14 categorías top-level del diseño ya existían en la BD (creadas en una fase anterior), pero ninguna tenía hijos — `product_category.parent_category_id` es una feature **nativa** de Medusa (no requiere módulo custom), simplemente no se había cargado la data. El storefront (`RodiCategoryChips`, `RodiMegaMenu`, `getCategoryByHandle` con `fields: "*category_children"`) ya estaba 100% cableado para renderizar subcategorías — no hizo falta tocar código de storefront ni de backend, solo crear las filas.
    - *Fix:* Se crearon 48 subcategorías vía `POST /admin/product-categories` (`parent_category_id` apuntando al padre) para los 8 categorías que el diseño define en `MEGAMENU`: Frescos, Despensa, Lácteos y huevos, Carnes y pescados, Bebidas, Aseo personal, Limpieza del hogar, Electrodomésticos. Despensa usa la lista explícita de `pages.jsx` (8 chips: Pasta y arroz, Granos y legumbres, Aceites y vinagres, Salsas, Enlatados, Harinas, Café y té, Endulzantes) en vez de los 7 de `MEGAMENU`, por ser la fuente más específica para esa categoría.
    - *Verificado:* `GET /store/product-categories?handle=despensa&fields=*category_children` devuelve los 8 hijos correctos; `/es/categories/despensa` (SSR) renderiza los 8 chips; `/es/categories/despensa-salsas` resuelve y muestra el h1 "Salsas" — confirma que el link "subcategoría → página propia" funciona end-to-end sin cambios de código.
    - *Gaps detectados en esta pasada, resueltos en un fix de seguimiento el mismo día (ver ítem siguiente):* conteo de productos no estaba en el hero, y navegar a una subcategoría hacía desaparecer el resto de los chips.
    - *Gap que sigue abierto:* 6 categorías sin taxonomía de subcategorías definida en el diseño (Panadería, Snacks y dulces, Congelados, Mascotas, Bebé, Farmacia — no aparecen en `MEGAMENU`); no se les creó subcategorías para no inventar taxonomía fuera del diseño de referencia. Tampoco hay productos reales categorizados bajo Despensa/sus subcategorías todavía (catálogo de demo sin categorizar) — el conteo por categoría es real pero muestra 0 hasta que se categoricen productos.
*   [x] **[UI/PLP-HERO]** El conteo de productos vivía solo en el toolbar del grid, no en el hero; y el selector de subcategoría tenía dos bugs de UX.
    - *Resuelto el:* 2026-07-10 por Claude, a pedido explícito tras el ítem anterior
    - *Fix 1 — conteo en el hero:* `RodiPlpHero` ahora acepta `productCount` y lo combina con `subtitle` en una sola línea ("N productos · descripción"), separado del breadcrumb (que ahora siempre se muestra, ya no depende de que `subtitle` sea truthy). `CategoryTemplate` calcula el conteo con una llamada liviana a `listProducts({ category_id: [category.id], limit: 1 }, ...)` en paralelo con `listBrands()`, antes del `Suspense` boundary.
    - *Fix 2 — los chips de subcategoría desaparecían al entrar a una:* causa raíz — `RodiCategoryChips` armaba la lista de chips a partir de `category.category_children`, pero al navegar a `/categories/despensa-salsas` el `category` pasado a la plantilla ES la subcategoría, que no tiene hijos propios (un nodo hoja), así que solo quedaba el chip "Todo". El intento inicial de resolver esto pidiendo `*parent_category.category_children` en la misma query a `/store/product-categories` **no funciona** — `query.graph()` no re-expande la relación inversa que ya se acaba de atravesar (child→parent→children vuelve vacío siempre, confirmado con `curl` directo). Fix real: cuando `category.parent_category` existe, `CategoryTemplate` hace una consulta separada `listCategories({ parent_category_id: parent.id })` para traer los hermanos, y usa al padre (no a la subcategoría) como `category` de `RodiCategoryChips` para que "Todo" apunte a la categoría padre.
    - *Fix 3 — el emoji del hero caía al genérico 📦 en una subcategoría:* `getCategoryVisual` solo tiene entradas para los 14 handles top-level; una subcategoría como `despensa-salsas` no matcheaba ni por handle ni por keyword de nombre ("salsas" no está en `EMOJI_BY_KEYWORD`). Fix: cuando hay `parent_category`, se resuelve el emoji con el handle/nombre del padre en vez del de la subcategoría.
    - *Verificado:* `/es/categories/despensa` — chip "Todo" activo (negro), 8 chips de subcategoría, emoji 🍝, "0 productos" en el hero (real, no placeholder). `/es/categories/despensa-salsas` — los 8 chips siguen presentes, el chip activo es "Salsas" (no "Todo"), emoji sigue siendo 🍝 (el de Despensa, no el genérico). `/es/categories/farmacia` (categoría sin subcategorías) sigue ocultando la fila de chips por completo. `/es/store` (hero genérico, sin `productCount`) sin regresión.
    - *Archivos:* `apps/storefront/src/modules/store/components/rodi-plp-hero/index.tsx`, `apps/storefront/src/modules/categories/templates/index.tsx`, `apps/storefront/src/lib/data/categories.ts`.
*   [x] **[CLEANUP]** Componente `filter-radio-group` sin uso en todo el codebase — **ya no aplica**, terminó reutilizado.
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Verificado como resuelto el:* 2026-07-11 por Claude, a pedido de Carlos (auditoría de backlog pendiente)
    - *Estado real:* [modules/common/components/filter-radio-group/index.tsx](apps/storefront/src/modules/common/components/filter-radio-group/index.tsx) ahora se importa y usa en [refinement-list/sort-products/index.tsx](apps/storefront/src/modules/store/components/refinement-list/sort-products/index.tsx) (título "Ordenar"), como parte del sidebar de filtros de marca/orden ya shippeado en la Fase 10. Ya no es código muerto — no hace falta ninguna acción.
*   [x] **[UI/FOOTER]** Faltan badges de métodos de pago (Visa/Mastercard/Discover) en el footer — **ya no aplica**, ya se agregaron.
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Verificado como resuelto el:* 2026-07-11 por Claude, a pedido de Carlos (auditoría de backlog pendiente)
    - *Estado real:* `layout/templates/footer/index.tsx:55-80` ya define un componente `PaymentBadges` (VISA, Mastercard-style, Discover-style) renderizado en la primera columna del footer (línea 101). No se encontró el commit/fecha exacta en que se agregó — quedó resuelto sin quedar registrado en este backlog en su momento.

## 🛠️ Implementaciones Pendientes & Mejoras
*   [ ] **[FEATURE/PLP-FILTROS]** El sidebar de filtros del listado de productos (PLP) solo tiene sort + marca; falta precio (2x1/Combos también pendiente, bloqueado por `[FEATURE/COMBOS]`).
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Actualizado el:* 2026-07-09 — **filtro de marca resuelto** (checkboxes en `rodi-plp-filters`, parámetro URL `brand_id`, endpoint `/store/products-list` con Index Module — ver [.context/plans/2026-07-08/FASE-10-filtro-marca-index-module.md](.context/plans/2026-07-08/FASE-10-filtro-marca-index-module.md) y [docs/custom-features/brands.md](docs/custom-features/brands.md)).
    - *Actualizado el:* 2026-07-18 por Claude, a pedido de Carlos — **Atributos, Calificación, Promociones (solo "En oferta") y la fila de chips de filtros activos con "Limpiar" por chip quedan resueltos**, reusando la arquitectura de `/store/products-list` de la Fase 10: `tag_id` filtra directo vía `query.graph()` (mismo módulo que `product`, no necesita Index Module — a diferencia de `brand`), `rating_gte`/`on_sale` se resuelven agregando en JS sobre un fetch liviano (reviews / `calculated_price` de todo el catálogo) e intersectando ids en `filters.id`, mismo patrón que `brand_id`. Nuevo script idempotente `apps/backend/src/scripts/seed-product-attributes.ts` carga 4 tags (`Orgánico`, `Sin gluten`, `Sin azúcar`, `Marca propia`) y los asigna a productos existentes. Archivos: `apps/backend/src/api/store/products-list/{route,validators,middlewares}.ts`, `apps/storefront/src/lib/data/tags.ts`, `apps/storefront/src/modules/store/components/{rodi-plp-filters,rodi-active-filter-chips,rodi-plp-toolbar}/index.tsx`, y el threading de `tag_id`/`rating_gte`/`on_sale` en `store/page.tsx`, `categories/[...category]/page.tsx`, `categories/templates/index.tsx`, `store/templates/index.tsx`, `paginated-products.tsx`. Queda pendiente correr `pnpm medusa exec ./src/scripts/seed-product-attributes.ts` contra un backend real y verificar en navegador (no ejecutado en esta sesión — ver .context/plans para el detalle de verificación pendiente).
    - *Impacto:* Del sidebar completo que pide el diseño (`pages.jsx:41-172`) solo falta **Precio** (slider de rango) y la parte de **Promociones** que depende de `[FEATURE/COMBOS]` ("2x1", "Combos"). El toggle de vista grid/lista y el orden "Más vendidos" del toolbar (`pages.jsx:110`) se evaluaron y se decidió dejarlos fuera — ver `[FEATURE/PLP-MAS-VENDIDOS]` para el caso de "Más vendidos".
    - *Nota backend (precio):* filtrar por precio es más difícil que marca — en Medusa v2 el precio no es un campo filtrable directo, se calcula en runtime por región/moneda vía el Pricing module (`variants.calculated_price`). Requiere investigación aparte sobre cómo exponerlo como filtro server-side antes de construir la UI (el patrón de agregación en JS usado para `rating_gte`/`on_sale` no aplica igual de bien a un rango continuo). 2x1/Combos siguen bloqueados por `[FEATURE/COMBOS]`, que no existe todavía.
    - *Posible solución (lo que queda):* Backend: investigar y resolver el filtro de precio (ver nota arriba) antes de construir la UI de slider.
*   [ ] **[FEATURE/PLP-MAS-VENDIDOS]** No existe orden por "Más vendidos" en el PLP, a pesar de aparecer en el toolbar del diseño (`pages.jsx:110`).
    - *Detectado el:* 2026-07-18 por Claude, investigando `[FEATURE/PLP-FILTROS]` a pedido de Carlos.
    - *Impacto:* `SortOptions` (`refinement-list/sort-products/index.tsx:5`) solo tiene `created_at`/`price_asc`/`price_desc`; el diseño muestra "Más vendidos" como opción de orden por defecto en el toolbar del PLP.
    - *Por qué no es una extensión chica del patrón de filtros:* a diferencia de `brand_id`/`tag_id`/`rating_gte`/`on_sale` (todos resueltos por intersección de ids sobre un fetch liviano), "Más vendidos" es un **orden**, no un filtro — no alcanza con intersectar un set de ids, hay que rankear todo el catálogo por un valor que hoy no existe en ningún lado. No hay agregación `SUM`/`GROUP BY` disponible vía `query.graph()`/`query.index()` en Medusa v2, y `order_line_item` no tiene link a `product` (solo campos de texto denormalizados). Requeriría: un job programado nuevo (`apps/backend/src/jobs/`) que recorra órdenes y calcule ventas por producto, un campo `sales_count` denormalizado para persistir ese conteo, y lógica de orden por ese ranking externo en `/store/products-list`. Tamaño comparable a un módulo custom nuevo, no una extensión del endpoint existente.
    - *Decisión:* Carlos decidió dejarlo fuera de la pasada de `[FEATURE/PLP-FILTROS]` (2026-07-18) y trackearlo acá como ítem propio a evaluar más adelante.
    - *Posible solución:* diseñar como feature separada cuando haya prioridad — job programado + campo denormalizado + extensión de orden en `/store/products-list`, siguiendo el patrón Module/Workflow del resto del repo si el conteo termina viviendo en un módulo custom.
*   [ ] **[FEATURE/PDP-TABS]** La sección de detalles de la PDP no tiene el formato de 4 tabs del diseño ni feature de Preguntas (Q&A).
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Impacto:* El diseño (`pdp.jsx:150-179`) especifica 4 tabs horizontales: "Descripción · Especificaciones · Reseñas (N) · Preguntas (N)". La implementación actual (`product-tabs/index.tsx`) es un acordeón de solo 2 ítems ("Información del producto", "Envío y devoluciones"). La tabla de especificaciones dedicada (`pdp.jsx:372-395`, key/value de material, origen, peso, dimensiones) está parcialmente cubierta dentro del acordeón pero no como tabla propia. El feature de "Preguntas" no existe en absoluto — no hay módulo backend ni UI (solo existen los módulos custom `brand` y `review`).
    - *Posible solución:* Evaluar si vale la pena replicar el formato de tabs horizontal (cambio de UI, no bloqueante) y, por separado, decidir si "Preguntas y respuestas" entra al roadmap como feature nueva (seguiría el mismo patrón Module → Link → Workflow → API que `review`).
*   [ ] **[UI/PDP]** Faltan elementos menores de la PDP presentes en el diseño: SKU, badge de oferta sobre la galería, mensaje de financiamiento en cuotas.
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Actualizado el:* 2026-07-11 — verificado que el botón de favorito sobre la galería **ya está resuelto** (`rodi-image-gallery/index.tsx:92-108`, toggle de corazón absoluto sobre la imagen principal, shippeado junto con el feature de Favoritos). Sigue pendiente todo lo demás de este ítem.
    - *Impacto:* `rodi-image-gallery/index.tsx` no muestra badge de "oferta" (`pdp.jsx:34-42`); `product-info/index.tsx` no muestra el SKU; no existe en ningún lado del storefront (PDP, carrito, checkout) mensajería de financiamiento tipo "4 cuotas sin interés" que el diseño incluye repetidamente (`pdp.jsx`, `pages.jsx`).
    - *Posible solución:* el badge de oferta puede reusar la misma lógica de `rodi-flash-sale`; SKU y financiamiento son agregados de UI simples si se define el proveedor de financiamiento (ej. Addi, mencionado en el diseño) — verificar si hay integración real prevista antes de mostrar mensajería falsa.
*   [ ] **[UI/HOME]** La sección "Marcas que amas" (`RodiBrandsStrip`) está oculta del home, sin fecha ni criterio definido para reactivarla.
    - *Detectado el:* 2026-07-17 por Claude, revisando el working tree (cambio de Carlos aún sin commitear al momento de esta auditoría).
    - *Estado actual:* en `apps/storefront/src/app/[countryCode]/(main)/page.tsx`, tanto el `import RodiBrandsStrip` como su render al final del home quedaron comentados (no eliminados), con una nota inline: *"Sección 'Marcas que amas' ocultada temporalmente (a pedido de Carlos, 2026-07-17). No se elimina — el componente RodiBrandsStrip queda disponible para reactivar en el futuro."* El componente en sí (`modules/home/components/rodi-brands-strip`) no se tocó ni se borró.
    - *Impacto:* el home ya no muestra la franja de marcas shippeada en Fase 6 ([.context/plans/2026-07-07/FASE-6-secciones-home.md](.context/plans/2026-07-07/FASE-6-secciones-home.md)) mientras esto siga así.
    - *Posible solución:* no hay acción de código pendiente en sí (la implementación ya existe) — falta que Carlos defina si esto es permanente (y entonces limpiar el import/render comentados) o temporal (y entonces definir cuándo/con qué criterio se reactiva).
*   [ ] **[UI/PDP-ENVIO]** El botón "Cambiar" de la tarjeta de envío en la PDP es un placeholder sin funcionalidad.
    - *Detectado el:* 2026-07-17 por Claude, a pedido de Carlos (mismo pedido en que se eliminó el cartel "Devolución gratuita" de esa tarjeta).
    - *Objetivo previsto de la función:* permitir al usuario **cambiar la dirección/zona de entrega** desde la propia ficha de producto, sin ir al checkout — patrón típico de e-commerce de mercado/grocery (Rappi/Rodi): el usuario fija su dirección y la tarjeta le muestra cuándo y cómo le llega a ESA dirección, con "Cambiar" para elegir otra. En el diseño de referencia (`apps/storefront/design-reference/ecommerce-test/pdp.jsx:113`) ese botón dice literalmente **"Cambiar dirección"** y acompaña a un texto de entrega personalizado por ubicación ("Llega mañana viernes · gratis · Pídelo antes de las 4 PM").
    - *Estado actual:* `apps/storefront/src/modules/products/components/rodi-pdp-delivery/index.tsx` — el botón es un `<button type="button">` (`RodiBtn kind="ghost"`) **sin `onClick`**, puramente decorativo. Además el copy actual ("Envío disponible / Calculado en el checkout") es genérico y **no** muestra estimación por dirección, así que hoy el botón no tiene una acción real que cambiar. Carlos decidió (2026-07-17) **dejarlo como placeholder** para abordarlo más adelante, no eliminarlo.
    - *Posible solución:* implementarlo de verdad conectándolo a un selector de dirección/zona que recalcule el envío estimado — requiere lógica de shipping options de Medusa (por región/zona) y persistir la ubicación del usuario (hoy no hay infraestructura de direcciones guardadas en el storefront fuera del checkout; ver también el ítem `[UI/HEADER]` ya resuelto donde se decidió NO construir un picker de direcciones en el header y se dejó solo el selector de región). Alternativa mínima si no se implementa: eliminar el botón para no mostrar un control muerto.
*   [ ] **[UX/MOBILE]** No existe bottom tab bar persistente en mobile; la navegación depende de un menú lateral (SideMenu).
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Impacto:* El diseño mobile (`mobile.jsx:47-74`, `MTabBar`) especifica una barra de navegación inferior persistente con 5 tabs: Inicio / Categorías / Pedidos / Favoritos / Mi cuenta. La implementación actual usa `side-menu/index.tsx`, un panel lateral tipo Popover disparado por un ícono de hamburguesa, con solo 4 links (Home, Store, Account, Cart) y sin Favoritos/Categorías/Pedidos como ítems de primer nivel. También faltan los FABs flotantes (back/heart/share) sobre el hero de la PDP mobile y el patrón de filtro/sort como bottom-sheet de dos botones (`mobile.jsx:642-645`) — hoy mobile colapsa el mismo sidebar de filtros (que de por sí solo tiene sort, ver ítem de filtros PLP).
    - *Posible solución:* Esto es una decisión de patrón de navegación (tab bar vs. menú lateral), no solo un componente faltante — conviene validarlo con producto/diseño antes de implementar, ya que implica rediseñar la navegación mobile completa.
*   [ ] **[UI/BUSQUEDA]** La página de resultados de búsqueda no tiene el hero de "sugerencia principal" ni pills de búsquedas relacionadas del diseño.
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Impacto:* El diseño (`pages.jsx:178-232`, `SearchPage`) muestra una card destacada con el "top suggestion" y una fila de pills de términos relacionados (ej. "leche entera", "leche deslactosada"). La implementación reutiliza `RodiSearchHero` + la misma grilla de productos que el listado normal, sin esos elementos.
    - *Posible solución:* Baja prioridad frente a favoritos y filtros — requiere lógica de sugerencias/términos relacionados que hoy no existe en ningún lado del backend.
*   [ ] **[FEATURE/NEWSLETTER]** Falta el backend real de suscripción al newsletter (persistencia + envío).
    - *Detectado el:* 2026-07-09 por Carlos
    - *Origen:* Contraparte del bug `[UI/HOME]` de arriba ("El newsletter del home no persiste el email suscrito"). Ese ítem cubre el arreglo mínimo honesto (dejar de mostrar éxito falso en el toast); este ítem es la implementación real que lo reemplaza de fondo. `rodi-newsletter/index.tsx` ya construye el formulario y el estado de envío — solo falta la persistencia.
    - *Impacto:* Sin esto, cualquier fix de UI al toast es un parche temporal — el negocio no está capturando leads de newsletter en absoluto.
    - *Posible solución:* Definir primero si se integra un proveedor externo de email marketing (ej. Mailchimp, Klaviyo, Resend) vía API, o si se guarda en una tabla propia del backend (nuevo módulo simple `newsletter-subscriber` siguiendo el patrón Module → Workflow → API de `AGENTS.md`, sin necesidad de link a producto/customer). La decisión de proveedor es de negocio, no técnica — bloquea el diseño del endpoint hasta resolverse. Una vez decidido: `POST /store/newsletter-subscriptions` (o el nombre que corresponda) + workflow con validación de email duplicado, y actualizar `rodi-newsletter/index.tsx` para llamarlo de verdad en vez del stub actual.

*   [ ] **[UI/HEADER]** La barra de navegación bajo el header (nav strip) muestra chips de categoría sin curar, tomando ciegamente las primeras 6 categorías top-level por orden de creación en vez de una lista editorial fija.
    - *Detectado el:* 2026-07-11 por Claude, a pedido de Carlos, comparando `rodi-header-client.tsx` contra `design-reference/ecommerce-test/shared.jsx:180-198`.
    - *Diseño de referencia:* botón "Todas las categorías" + fila fija de chips curados: `Ofertas del día · Frescos · Despensa · Aseo · Limpieza · Electrodomésticos · Mascotas · Marcas propias` — una selección editorial fija, no "las primeras N categorías que existan en la BD".
    - *Implementación actual:* [rodi-header-client.tsx](apps/storefront/src/modules/layout/components/rodi-header/rodi-header-client.tsx) combina `QUICK_LINKS` hardcodeado (`Ofertas del día`, `Todos los productos`) con `navCategories = categories.filter(c => !c.parent_category).slice(0, 6)` — sigue sin filtrar ni ordenar por curación.
    - *Actualizado el:* 2026-07-17 — **la parte "mezclando categorías demo de ropa" quedó parcialmente obsoleta.** Los commits `0fc1f88`/`f2f6bee` (Carlos) consolidaron Shirts/Sweatshirts/Pants/Merch (antes 4 categorías top-level sueltas) en subcategorías de una nueva categoría padre **"Ropa"**, que además fue promovida a categoría curada de pleno derecho: tiene su propia entrada en `CATEGORY_VISUAL_BY_HANDLE` (foto Unsplash, emoji 👕, tagline "Moda para toda la familia") igual que las 14 categorías de supermercado — ya no es "basura de demo", es una 15ª categoría real del catálogo. Efecto en este ítem: `navCategories.slice(0, 6)` ahora antepone 1 categoría legítima ("Ropa") en vez de 4 demo, lo cual reduce el síntoma pero **no resuelve la causa** — el slice sigue sin ser una lista curada, y "Ropa" tampoco aparece en la lista fija del diseño de referencia citada arriba (que no contempla ropa/moda). Sigue pendiente decidir: ¿"Ropa" debería entrar al nav strip curado, o el diseño de referencia (pensado 100% para grocery) necesita extender su lista fija para dar lugar a esta nueva categoría de negocio?
    - *Qué suele aparecer ahí en ecommerces reconocidos:* comparando con Amazon, MercadoLibre y apps de grocery delivery (Rappi, Instacart, Walmart) — el patrón típico es un disparador fijo de "Todas las categorías/Departamentos" + una fila corta de atajos **curados**: ofertas/promos del día, las categorías de mayor tráfico, marca propia/private label, y a veces algo personalizado. Casi nunca incluyen un link genérico tipo "Todos los productos" ahí, porque es redundante con el botón de categorías y con el buscador.
    - *Posible solución:* (1) quitar `"Todos los productos"` de `QUICK_LINKS` por redundante; (2) reemplazar el `slice(0, 6)` ciego por una lista curada explícita de handles (mismo set que ya vive en `CURATED_CATEGORY_HANDLES` en [category-emoji.ts](apps/storefront/src/lib/util/category-emoji.ts), hoy 15 con "ropa" incluido) — decidiendo primero si "Ropa" entra a esa lista curada específica del nav strip o se excluye a propósito por no estar en el diseño original.
    - *Nota:* solo diagnóstico por ahora, no implementado — pendiente de decisión de Carlos sobre si aplicar el fix.

*   [ ] **[FEATURE/COMBOS]** No existe ninguna lógica de combos/bundles de productos en el proyecto.
    - *Detectado el:* 2026-07-09 por Carlos
    - *Origen:* Búsqueda de "combo"/"bundle" en `apps/backend/src` y `apps/storefront/src` no arroja resultados — el único rastro es el mockup visual de `apps/storefront/design-reference/`: panel "Combo guacamole · 4 productos" en la PDP (`pdp.jsx:497-506`, con precio combo + precio tachado + botón "Ver combo →"), sugerencia cross-sell "Combo helado + galletas" en mobile (`mobile.jsx:402-406`), y el filtro "Combos" del sidebar de PLP (`pages.jsx:71`, ya referenciado sin definir en el ítem `[FEATURE/PLP-FILTROS]` de arriba). No hay módulo, modelo, link, workflow ni endpoint — es del mismo tamaño que Favoritos antes de implementarse.
    - *Impacto:* Sin esto no se puede mostrar el panel de combo en la PDP, la sugerencia cross-sell, ni el filtro "Combos" de la PLP (que hoy queda huérfano en el backlog de filtros por falta de este concepto).
    - *Posible solución:* Medusa v2 tiene un recipe oficial para esto — [Bundled Products Recipe](https://docs.medusajs.com/resources/recipes/bundled-products), con ejemplo de código en [medusajs/examples/bundled-products](https://github.com/medusajs/examples/blob/main/bundled-products/README.md). Recomienda el mismo patrón `Module → Link → Workflow → API` ya usado en este repo (`brand`/`review`/`favorite`): módulo custom con modelos `Bundle` y `BundleItem`, enlazados a `Product` vía module link; workflows para crear el bundle y para agregarlo al carrito (expandiendo cada `BundleItem` como línea de carrito relacionada, con su propio manejo de precio e inventario). **Importante**: el inventory kit nativo de Medusa (agrupar variantes a nivel de inventario) no sirve para esto — no permite precio distinto para el combo ni fulfillment separado de sus ítems — por eso el recipe descarta esa vía y exige el módulo custom.

*   [ ] **[FEATURE/ZONAS-ENTREGA]** No existe manejo de disponibilidad de producto por zona de entrega (Provincia/Municipio); el "Entregar en" del header es estático.
    - *Detectado el:* 2026-07-19 por Claude, a pedido de Carlos (análisis de factibilidad para adaptar el multi-región de Medusa a un solo país acotado por Provincias/Municipios, mostrando en el PLP solo los productos disponibles en la zona elegida).
    - *Origen:* El label "Entregar en" existe solo como mockup estático (`design-reference/ecommerce-test/shared.jsx:144-155`, `mobile.jsx:27-32`) — dirección fija, sin lógica ni lista de zonas. En la implementación real el único selector geográfico es `CountrySelect` (país, `rodi-header-client.tsx:73-88`). El backend acepta el param `province` en `/store/products-list` pero lo **descarta** (`middlewares.ts:83`, `clearFiltersByKey`) — hoy no filtra catálogo. No hay ningún seed de provincias/municipios de ningún país (el seed real es Europa/EUR; el diseño asume Colombia/COP — inconsistencia a resolver primero).
    - *Impacto:* Sin esto no se puede ofrecer el patrón grocery/marketplace (Rappi/Rodi) de "elegí tu zona → ves lo que te llega a esa zona", que el diseño de "Rodi Mercado" asume en varias superficies (header, badge "El más comprado en tu zona" `pages.jsx:203`, tarjeta de envío de PDP con botón "Cambiar", ver `[UI/PDP-ENVIO]`).
    - *Análisis y plan:* **Factible**, pero NO reutilizando el módulo `Region` de Medusa (Region = moneda+tax+pago, no área de entrega; la región debe seguir siendo una sola). Enfoque elegido (B): módulo custom `zone` (Provincia→Municipio) + link N–M producto↔municipio filtrable vía Index Engine + selector real "Entregar en" persistido en cookie + filtro `zone_id` en `/store/products-list` (mismo patrón que `brand_id` de la Fase 10), con geo_zones sub-país para validar entregabilidad. Plan completo en fases (0: repunte de región a Cuba, A: backend, B: storefront, C: checkout, D: admin), en [`plan-zonas-entrega-provincia-municipio.md`](./plan-zonas-entrega-provincia-municipio.md).
    - *Decisiones de negocio cerradas (2026-07-19 por Carlos):* **País = Cuba** (definitivo; 15 provincias + Isla de la Juventud como pseudo-provincia). **Región se repunta a Cuba SIN migración de moneda** — CUP no se usa, precios se quedan en EUR/USD intactos (Fase 0, liviana, no destructiva). **Disponibilidad permisiva** (producto sin zona = disponible en todas). **Fase C (checkout) entra.** **Demo pero production-close.**
    - *Progreso de ejecución:*
        - **Fase 0 (repunte de región a Cuba):** ✅ aplicada y verificada 2026-07-20 (seed fuente de verdad + `migrate-store-to-cuba.ts` in-place + `DEFAULT_REGION=cu`; `/cu/store` renderiza 48 productos en EUR; precios intactos, sin CUP).
        - **Fase A (backend de disponibilidad por zona):** ✅ implementada y verificada 2026-07-21. Módulo `zone` (`Province`/`Municipality`), link N–M **plano** `product-municipality`, workflows `create-zones`/`set-product-zones`, `GET /store/zones`, filtro `zone_id` en `/store/products-list`, `seed-zones.ts` (16 provincias / 168 municipios). **Mejora sobre el plan:** el filtro de zona NO usa Index Engine ni link `filterable` — resuelve con `query.graph()` + fallback permisivo en JS (patrón `rating_gte`/`on_sale`), así que **no hay reindex** atado a zonas. Verificado (curl + Playwright): La Habana→48, Pinar del Río→46 (2 restringidos ocultos). Rutas admin de zonas → Fase D.
        - **Fase B (storefront: selector "Entregar en" + filtrado):** ✅ implementada y verificada 2026-07-21. `lib/data/zones.ts` (cookie `_charlie_zone` + server action `setActiveZone`), `zone_id` en `listProducts` (salvo fetch por `id`/`handle`), componente `rodi-zone-picker` (dropdown custom Headless UI Listbox, Provincia→Municipio, orden geográfico) que **reemplaza** `CountrySelect` en header + side-menu (país único → país/moneda queda en la top bar). Verificado en navegador: elegir municipio filtra la grilla (Pinar del Río→46, La Habana→48), persiste tras reload (cookie), 0 errores.
        - **Fase C (entregabilidad en checkout):** ✅ implementada y verificada 2026-07-21. Cobertura por provincia (16 geo-zones `type:"province"` ISO 3166-2) + geo-zone país `cu` como gate robusto (el `province` del checkout es texto libre); envío plano. Script `migrate-fulfillment-to-provinces.ts`. Puente zona→checkout **en cliente**: `activeZone` prellena País/Provincia/Municipio del form de dirección (no mutación de carrito — evita el auto-`country_code` de Medusa y la lectura stale de `revalidateTag` intra-request). Verificado Playwright: checkout prellenado (Cerro, La Habana) + paso `delivery` muestra Standard/Express.
        - **Auditoría 2026-07-22:** relación zona↔direcciones guardadas revisada a pedido de Carlos — ver [`reports/zonas-entrega-direcciones-incongruencias-2026-07-22.md`](./reports/zonas-entrega-direcciones-incongruencias-2026-07-22.md) para las incongruencias detectadas. Mitigación parcial esa misma fecha: los formularios de dirección (perfil y checkout) pasaron `province`/`city` de texto libre a selects Provincia→Municipio contra `/store/zones`, se quitó el selector de país (Cuba fija) y los campos Empresa/Apartamento, y se agregó "Nota para la entrega (opcional)". La dirección de facturación se independizó del envío, se auto-carga del perfil, y tiene su propio select de país (cualquiera, no solo Cuba).
        - **Plan de cierre ejecutado el 2026-07-23** (`.claude/plans/arma-un-plan-para-abundant-shell.md`, aprobado por Carlos tras confirmar 4 decisiones: geo-zones solo corrección técnica sin matriz de tarifas, enforcement zona↔carrito con aviso blando + limpieza automática del carrito al continuar, Fase D solo el widget de asignación sin CRUD de Provincia/Municipio, picker y checkout sincronizados en ambos sentidos sin rediseño). Cuatro workstreams, todos completados:
          - **A (zona↔carrito, picker):** nuevo endpoint `POST /store/zones/eligibility-check` (lógica compartida extraída a `modules/zone/utils/product-eligibility.ts`, reutilizada también por el filtro `zone_id` de `/store/products-list`). El picker "Entregar en" ahora recibe los ítems del carrito (hilados desde `RodiHeader`, que ya hacía `retrieveCart()`); si cambiar de zona deja productos no disponibles, un diálogo (`zone-conflict-dialog`, nuevo componente común) avisa y, al confirmar, los quita del carrito (`deleteLineItem`) antes de aplicar la zona.
          - **B (zona↔checkout):** `setAddresses` (`lib/data/cart.ts`) reescrito con un estado de retorno `SetAddressesState` (discriminated union: `idle`/`error`/`zone-conflict`) en vez de un string plano; resuelve el municipio elegido, chequea elegibilidad contra el mismo endpoint, y si hay conflicto no confirma el guardado — la UI (`addresses/index.tsx`) muestra el mismo diálogo y reenvía el formulario con un campo oculto `confirm_zone_change` (patrón `useActionState` + `requestSubmit()` en un `useEffect`, ya que no hay confirm nativo mid-submit con Server Actions). Al confirmar, se recalculan (no se confía en el cliente) y quitan los ítems no elegibles, y la cookie `_charlie_zone` se sincroniza con el municipio guardado en **cualquier** guardado exitoso de dirección (no solo el de conflicto). De paso se corrigió un bug latente (`getCartId()` sin `await`).
          - **C (Fase D, admin):** nuevo widget `admin/widgets/product-zones.tsx` (FocusModal, checkboxes agrupados por provincia) en el detalle de producto, conectado a `setProductZonesWorkflow` (ya existía, solo la invocaba `seed-zones.ts`) vía rutas nuevas `GET/POST /admin/products/:id/zones` + `GET /admin/zones` (mirror de `/store/zones`, no cruza el auth boundary store/admin). Sin CRUD de Provincia/Municipio (decisión: son datos fijos).
          - **D (geo-zones):** `Province` ganó `iso_code` (migración nueva) poblado desde una única fuente de verdad (`modules/zone/constants.ts::CUBA_PROVINCE_ISO_CODE`, keyed por `code` en vez de por nombre). Las 16 geo-zones de provincia (inertes, ver `geo-zones-fulfillment.md`) se sacaron de `initial-data-seed.ts` y se agregó `scripts/remove-inert-province-geo-zones.ts` (idempotente) para limpiar ambientes ya migrados; `migrate-fulfillment-to-provinces.ts` quedó marcado histórico/superado en su propio docblock. **No** se implementó diferenciación real de tarifas por provincia (matriz de negocio, fuera de alcance por decisión explícita).
          - Verificado: `tsc --noEmit` limpio en backend y storefront (solo 2 errores preexistentes no relacionados, confirmados antes de esta sesión).
        - **Documentado el 2026-07-23**: doc de referencia nueva [`docs/custom-features/zones.md`](../docs/custom-features/zones.md) (mismo formato que `brands.md`/`reviews.md`/`favorites.md`, cubre módulo/link N–M/workflows/rutas/widget admin/storefront/aviso blando de los 4 workstreams), agregada a la tabla "Funcionalidad custom actual" de `AGENTS.md` y al índice de `docs/README.md`.
        - **Fix de seguimiento 2026-07-24** (a pedido de Carlos): el aviso blando del checkout (Workstream B) quitaba del carrito los ítems no disponibles en la zona confirmada, pero si **todos** los ítems del carrito quedaban restringidos, `setAddresses` seguía guardando la dirección y redirigiendo a `step=delivery` — dejaba al cliente en un checkout con el carrito vacío, sin explicación. Fix: `setAddresses` (`lib/data/cart.ts`) relee el carrito (`retrieveCart`) justo después de los `deleteLineItem` de la rama confirmada; si quedó vacío, se salta el `updateCart` y redirige a `/cu/cart?zone_emptied=true` en vez de a `step=delivery` (la cookie de zona igual se sincroniza). Nuevo componente `modules/cart/components/zone-emptied-notice/index.tsx` (montado en `CartTemplate`) lee ese querystring y muestra un toast explicando por qué el carrito quedó vacío — necesario porque el `redirect()` server-side no puede devolver el mensaje como estado de `useActionState` (la ejecución corta antes de que el componente reciba nada). `tsc --noEmit` limpio en storefront tras el cambio. Detalle en `docs/custom-features/zones.md`.
        - **Fix de seguimiento 2026-07-24 (UI)**, a pedido de Carlos: `ZoneConflictDialog` (el aviso blando de arriba, compartido por picker y checkout) solo mostraba el nombre del producto en la lista de ítems a quitar — se agregó la foto (`Thumbnail`, mismo componente del carrito, `size="square" variant="rodi"`). `ZoneConflictItem`/`ZoneCartItem` ganaron un campo `thumbnail`, hilado desde `item.thumbnail` del carrito en ambos puntos de entrada (`rodi-header/index.tsx` para el picker, `findIneligibleLineItems` en `lib/data/cart.ts` para el checkout). `tsc --noEmit` limpio.
        - **Sigue pendiente** (fuera de alcance de este plan, no evaluado): botón "Cambiar" de la PDP (`[UI/PDP-ENVIO]`) sin conectar al picker de zona; unificación de IA entre el picker y el selector de checkout (se mantuvieron como widgets separados por decisión explícita); sincronizar la zona activa con la dirección default del cliente al iniciar sesión sin cookie.
        - **Geo-zones de fulfillment:** documentado a detalle en [`geo-zones-fulfillment.md`](./geo-zones-fulfillment.md), actualizado 2026-07-23 tras la corrección de deuda técnica — el envío sigue siendo tarifa plana idéntica en toda Cuba (decisión consciente), pensado para retomarse cuando haya una decisión de negocio sobre tarifas/tiempos diferenciados por zona.
    - *Posible solución:* ver el plan. Fases 0, A, B y C hechas; queda la Fase D (admin).

## 🩹 Deuda Técnica y Refactorizaciones Minoritarias
*   [ ] **[DATA/REVIEWS]** No hay constraint ni validación que impida que un mismo customer deje varias reseñas sobre el mismo producto.
    - *Detectado el:* 2026-07-09 por Carlos
    - *Origen:* Ya documentado como pendiente en [docs/custom-features/reviews.md:100](docs/custom-features/reviews.md#L100) (tabla "Extender Reviews"), no es un hallazgo nuevo — se traslada aquí para que quede trackeado como ítem de backlog accionable en vez de solo mencionado en la doc de referencia.
    - *Impacto:* `createReviewStep` ([workflows/steps/create-review.ts](apps/backend/src/workflows/steps/create-review.ts)) inserta directamente sin verificar si ya existe una reseña de ese `customer_id` para ese `product_id`, y la migración de `review` ([modules/review/migrations/](apps/backend/src/modules/review/migrations/)) no tiene ningún índice único sobre `(product_id, customer_id)`. Un mismo cliente puede dejar N reseñas del mismo producto, infla el promedio/conteo que muestra `RodiProductReviews` y el endpoint `/store/reviews/summary`, y no hay forma de que la UI ofrezca "editar tu reseña" en vez de "crear otra" porque no hay unicidad que lo garantice.
    - *Posible solución:* Agregar un índice único compuesto `(product_id, customer_id)` en una nueva migración (no editar la migración inicial ya aplicada), y validar en `createReviewStep` (o antes, en la API route) si ya existe una reseña del customer para ese producto — devolver un error claro (ej. 409) en vez de dejar que la constraint de DB reviente como error 500 genérico. Nota: el plan pendiente de Favoritos (aún no implementado) ya contempla agregar este mismo tipo de constraint único desde el inicio en `favorite`, precisamente para no repetir este gap ahí también.
*   [ ] **[UI/PDP]** Los selectores de variante de color son botones de texto, no círculos de color (swatches).
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Impacto:* `rodi-option-select/index.tsx` usa el mismo estilo de botón para todas las opciones de variante; el diseño (`pdp.jsx:287-301`) muestra círculos de color reales para la opción "Color". Gap visual menor, sin impacto funcional mientras no haya productos con variantes de color en catálogo.
    - *Posible solución:* Diferenciar el render de `rodi-option-select` cuando el nombre de la opción sea "Color" (o similar), usando el valor como color de fondo del swatch.
*   [ ] **[UI/MEGAMENU]** El panel de promoción del mega menú tiene contenido hardcodeado en vez de una promoción real activa.
    - *Detectado el:* 2026-07-08 por Claude, en auditoría contra `design-reference`
    - *Impacto:* `rodi-mega-menu/index.tsx` muestra "Ofertas del día" y un badge estático de "2x1" sin relación con promociones reales del catálogo. Menor, pero relacionado con la falta general de un concepto de "promoción" en el backend (ver ítem de filtros PLP).
    - *Posible solución:* Depende de que exista un concepto real de promoción/oferta en el backend antes de poder mostrar contenido dinámico aquí.
*   [ ] **[UX/FAVORITOS]** Los errores de "no logueado" en Favoritos no son descriptivos para el usuario final.
    - *Detectado el:* 2026-07-10 por Carlos
    - *Impacto:* Dos casos distintos, ambos con el mismo problema de fondo (mensaje genérico en vez de explicar que hace falta iniciar sesión):
      1. Al intentar marcar un producto como favorito estando deslogueado, se muestra "Unauthorized" en vez de un mensaje claro. `addFavorite`/`removeFavorite` ([lib/data/favorites.ts](apps/storefront/src/lib/data/favorites.ts)) ya tienen un guard explícito que debería devolver `"Inicia sesión para guardar tus favoritos."` cuando no hay `authorization` en los headers — a investigar por qué no está disparando en este caso (¿cookie de sesión inválida/expirada que sí matchea el guard pero el backend igual rechaza con 401 crudo, cuyo `error.message` se muestra tal cual en el `catch`?).
      2. Al navegar directo a `/account/favorites` deslogueado, se muestra la página 404 genérica de Next.js en vez de redirigir a login o explicar que la función requiere cuenta. Esto **no es específico de favoritos** — `favorites/page.tsx` solo replica el mismo patrón `if (!customer) notFound()` que ya usan `addresses/page.tsx`, `orders/page.tsx`, `profile/page.tsx` y el resto de `@dashboard/*` — es una convención existente en todo el módulo de cuenta, no un bug introducido por el feature de favoritos.
    - *Posible solución:* (1) Para el toggle: confirmar en qué escenario `getAuthHeaders()` deja pasar el guard pero el backend igual devuelve 401, y mapear esa respuesta a un mensaje amigable también. (2) Para la página: es una decisión de UX más amplia para todo `account/@dashboard` (redirigir a `/account` con mensaje, en vez de `notFound()`) — si se aborda, conviene resolverlo para todas las páginas del dashboard a la vez, no solo favoritos, para no dejar el resto inconsistente.
